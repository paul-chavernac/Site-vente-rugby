{# empty Twig template #}
{% extends "base.html.twig" %}
{% block body %}


    <div class="container-fluid font-fam-courrier" style="margin-top: 50px;" >
        <div class="row">
            <div class="col-sm-3 offset-1">
                <img class="img-fluid img-thumbnail" style="height: 400px; width: 300px;" src="{{produit.getProImage}}" >
            </div>
            <div class="col-sm-6">
                <h1>{{produit.getProNom}}</h1>
                <br>
                <h3>{{produit.getProPrix}} € </h3>
                <h4>{{produit.getProStock}} encore en stock </h4>
                <br/>
                <h5>{{produit.getProResume}}</h5>
                <br>
                {% if not is_granted('ROLE_ADMIN') %}
                    <a class="btn btn-dark" href="{{ path('ajoutPanierFromProduit', { 'id': produit.getProId }) }}" >
                        <i class="fas fa-shopping-basket"></i> Ajouter au panier</a>
                {% endif %}

                {#                <a class="btn btn-danger"><i class="fas fa-heart"></i> Ajouter aux favoris</a>#}

            </div>
        </div>
    </div>

{%endblock%}